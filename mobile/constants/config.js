// ── Block & Flow Defaults ─────────────────────────────────────────────────────
export const BODY_SCAN_BLOCK_ID = 20
export const DEFAULT_BLOCK_SECS = 60
export const DEFAULT_INTERSTITIAL_SECS = 20
export const STREAK_THRESHOLD_SECS = 300 // 5 minutes — sessions under this don't count toward streak

// ── Music ─────────────────────────────────────────────────────────────────────
export const PREVIEW_DURATION_MS = 15000
export const FADE_IN_MS = 2000
export const FADE_CROSS_MS = 800
export const FADE_OUT_MS = 2000

// ── UI Timing ─────────────────────────────────────────────────────────────────
export const CONTROLS_OVERLAY_TIMEOUT_MS = 3000
